#include "utility.h"
// isomorphism.cpp, or anagram, related with set property. see also " unique.cpp

// check if two array are isomorphic, eg, two string.
// non-isomorphic if length differ.
// sort two array then compare solution.
// use two counter array, or one diff counter array, based on value, applicable for small char set.
// use two map/dictionary.

int main() {
    test::init();
}
