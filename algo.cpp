#include "utility.h"  // cannot include <algorithm>, ambiguous overload
// "algo, see also " tp(two pointer)
// adjacent_find -> unique
// is_sorted_until
// std::adjacent_find return first of the first pair of identical elements or last.
        // std::partial_sum(psum.begin(), psum.end(), psum.begin());
        // std::accumulate(psum.begin(), psum.end(), 0);

int main() {
    test::init();
}
